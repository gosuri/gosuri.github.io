.sass-cache*
Gemfile.lock
_site*
.jekyll-cache
.DS_Store

# Subagent-driven development scratch (ledgers, briefs, review packages)
.superpowers/

transcripts/

# Python bytecode from the research pipeline
__pycache__/

# Node deps for the card renderer (installed fresh in CI)
research/node_modules/

# Pipeline run logs. Whisper runs with verbose output, so these embed
# verbatim third-party transcript text — same reason transcripts/ is ignored.
catalog/*.log
catalog/**/*.log
